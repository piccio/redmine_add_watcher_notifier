<%= l(:added_as_watcher, issue: @issue.id, author: @action_author, scope: :add_watcher_notifier) %>

----------------------------------------
<%= render partial: 'issue', formats: [:text], locals: { issue: @issue, user: @user, issue_url: @issue_url } %>